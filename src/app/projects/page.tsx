import { Metadata } from "next";
import ProjectsPage from "../_components/ProjectsPage";

export const metadata: Metadata = {
  title: "Projects | Parag Naikade",
};

export default function Page() {
  return <ProjectsPage />;
}
